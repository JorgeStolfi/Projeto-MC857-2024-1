import html_pag_ver_comentario_IMP

def gera(ses, com, erros):
  """Retorna uma página HTML que mostra os dados do comentário {com}
  (que deve ser um objeto de tipo {obj_comentario.Classe}).  
  
  O parâmetro {ses} deve ser um objeto da classe {obj_sessao.Classe}
  que representa a sessão corrente."""
  return html_pag_ver_comentario_IMP.gera(ses, com, erros)
