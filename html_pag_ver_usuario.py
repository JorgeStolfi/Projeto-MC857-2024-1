import html_pag_ver_usuario_IMP

def gera(ses, usr, erros):
  """Retorna uma página HTML que mostra os dados do usuário {usr}
  (que deve ser um objeto de tipo {obj_usuario.Classe})."""
  return html_pag_ver_usuario_IMP.gera(ses, usr, erros)
