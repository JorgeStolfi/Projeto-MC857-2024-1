import html_pag_ver_video_IMP

def gera(ses, vid, erros):
  """Retorna uma página HTML que mostra os dados do video {vid}
  (que deve ser um objeto de tipo {obj_video.Classe}).  
  
  O parâmetro {ses} deve ser um objeto da classe {obj_sessao.Classe}
  que representa a sessão corrente.
  
  A página terá botões "Ver conversa", "Editar", e "Comentar", conforme
  apropriado."""
  return html_pag_ver_video_IMP.gera(ses, vid, erros)
