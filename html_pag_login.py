
import html_pag_login_IMP

def gera(erros):
  """Retorna uma página contendo o formulário de entrar (login).
  Veja {html_form_login.gera}.  
  
  Se {erros} não é None, deve ser uma lista de mensagens de erro
  a incluir na página retornada."""
  return html_pag_login_IMP.gera(erros)
