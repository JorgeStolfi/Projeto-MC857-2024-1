import html_estilo_button_IMP

def gera(cor_fundo):
  """Retorna um fragmento CSS para uso no atributo "style" de um botão
  (simples ou submit). Especifica a {cor_fundo} indicada,
  com fonte, tamanho etc. padronizados."""
  return html_estilo_button_IMP.gera(cor_fundo)

def escolhe_cor_fundo(texto):
    return html_estilo_button_IMP.escolhe_cor_fundo(texto)
    
    
