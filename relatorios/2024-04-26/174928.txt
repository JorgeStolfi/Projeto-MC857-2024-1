174928 comando_ver_usuario: se o usuário que está sendo exibido é o dono da sessão,
                            omitir os botões "Ver..." no pé da página pois já tem no menu principal.

Foram realizadas as seguintes alterações:

    1.  Correção de texto do comentário descritivo da função "processa()", no arquivo "comando_ver_usuario.py".
    
    2.  Remoção de import duplicado no arquivo "comando_ver_usuario_IMP.py".
    
    3.  Omissão dos botões "Ver..." no pé da página quando o usuário exibido é o dono da sessão.
        Para isso foi necessário alterar o arquivo "html_bloco_dados_de_usuario_IMP.py",
        pois é nele que é criado o bloco contendo as informações exibidas na página, inclusive os botões.
        A remoção dos botões foi feita adicionando-se uma checagem condicional,
        que confere a informação "ses_proprio".