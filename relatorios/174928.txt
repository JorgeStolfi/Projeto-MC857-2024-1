# relatorio da aula de 2024-06-07

Tarefa: O objeto {obj_usuario.Classe} tem um novo atributo, 'vnota', a nota média dos vídeos postados pelo usuário.
        Modifique {html_linha_resumo_de_usuario_IMP.py} e {html_bloco_lista_de_usuarios_IMP.py} para mostar esse atributo também.

Modificações em {html_linha_resumo_de_usuario_IMP.py}:

    1.  Foi adicionado o campo 'vnota' na lista "colunas".

    2.  Foi adicionado o tratamento condicional para quando a chave é o novo campo 'vnotas' adicionado na lista "colunas".
        Nesse caso, o booleano "mostra" foi definido como verdadeiro, junto com a nota a ser exibida e o texto que aparece no cabeçalho quando o atriubto usr é nulo.

    3 . O teste de {html_linha_resumo_de_usuario} finalizou sem erros.

Modificações em {html_bloco_lista_de_usuarios_IMP.py}:

    1.  Não foi necessário fazer modificações nesse arquivo, visto que ele constroi o bloco de usuários a partir do módulo {html_linha_resumo_de_usuario}, que já foi modificado.
    
    2. O teste de {html_bloco_lista_de_usuarios} finalizou sem erros, e nele foi possível visualizar no cabeçalho da tabela o texto definido no módulo {html_linha_resumo_de_usuario}, para quando o atributo usr é nulo.