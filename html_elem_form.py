import html_elem_form_IMP

def gera(conteudo):
  """Retorna um string que é um fragmento HTML consistindo de um <form>...</form>
  com método "post" e o {conteudo} dado.  Este deve ser um fragmento HTML com os 
  elementos "<input>" necessários.
  Note que um elemento "<form>" não pode ocorrer dentro de um parágrafo."""
  return html_elem_form_IMP.gera(conteudo)
