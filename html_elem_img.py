import html_elem_img_IMP

def gera(nome, alt, tam):  
  """Constrói o HTML para um elemento "<img ...>" 
  que mostra a imagem "imagens/{nome}" com altura {tam} 
  e texto alternativo {alt}."""
  return html_elem_img_IMP.gera(nome, alt, tam)
